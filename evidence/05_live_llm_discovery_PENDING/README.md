# 05 — Live LLM discovery (PENDING)

**Status: not yet performed. No evidence exists in this folder or
anywhere else in this repository for a genuine LLM-driven discovery run.**

This assignment's discovery mechanism (`cuas.discovery`, exercised so far
only against a `FakeLLM`/deterministic test double in the unit and
integration suites) has not yet been run against a real Anthropic API key
and a real model. That run is still required and is deliberately being
done as its own, separate step:

- No screenshot, log, trace, or output in this repository is a stand-in
  or substitute for that run.
- Nothing here has been generated by a mock, a fake, or a hand-written
  script and labeled as if it came from a real model.
- This folder intentionally contains only this README until a real
  Anthropic API key is provided and a genuine discovery run is executed
  and captured.

When that run happens, this folder will be populated the same way
scenarios 01–04 were: real, unmodified logs/traces from the actual run,
with a README explaining exactly what was captured and what, if anything,
still could not be verified.
