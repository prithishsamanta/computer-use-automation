__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/
.env
data/*.db
data/*.sqlite3
# Runtime evidence/logs can contain real PII (member IDs, unredacted
# screenshots -- see cuas.observability.evidence's module docstring).
# data/artifacts/ IS committed on purpose (reviewable versioned
# artifacts); these are not.
data/evidence/
data/logs/
# Discovery traces are redacted but are still runtime output of real
# discovery runs (unlike data/artifacts/, which is reviewable and
# versioned on purpose) -- not committed, same as evidence/logs.
data/discovery_traces/
.DS_Store
node_modules/
